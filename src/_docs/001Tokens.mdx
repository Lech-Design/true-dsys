import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { DesignTokenDocBlock } from "storybook-design-token";
import "../styles/docs/global-tokens_docs.css";

<Meta title="Tokens/Colour" />

{/* <img className="sg-img" src="assets/soa_logo_crop.png" /> */}

# Colour tokens

Design tokens are the visual design atoms of the design system — specifically, they are named entities that store visual design attributes. We use them in place of hard-coded values (such as hex values for color or pixel values for spacing) in order to maintain a scalable and consistent visual system for UI development.

### Base tokens

These tokens define the core of True, they are referenced by the MudBlazor theme tokens.

<DesignTokenDocBlock
  categoryName="Base Colors"
  maxHeight={800}
  viewType="table"
/>
### MudBlazor theme tokens{" "}

These tokens are theme overdides for the default MudBlazor tokens, this stylesheet is automatically generated by MudBlazor.

Use the following build command to transpile the tokens into a MudBlazor comptible stylsheet (TODO- Add markup)

<DesignTokenDocBlock categoryName="Colors" maxHeight={4000} viewType="table" />;
